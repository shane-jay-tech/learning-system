# learning pytest「384/7」口径澄清留档（2026-09-19 晨间执行班，任务 l918-01，零改动）

> 结论先行：**「384 passed, 7 skipped」中的 7 是 skipped 不是 failed**——零失败。7 条 skip 全部是 g++／Rscript 未安装的环境条件跳过（`skipif`），与本机代码质量无关；解除方式＝日间安装 g++ / Rscript 后自动转为正常执行。

## 一、复跑命令（2026-09-19 07:5x 实测，输出与 9/18 复核一致）

```bash
python -m pytest tests -q -rs
# → 384 passed, 7 skipped in 35.47s   （零 failed）
# -rs 会逐条打印 skip 原因，输出 7 行 SKIPPED，见下表
```

## 二、7 条 skip 的 file:line 表（-rs 实测输出）

| # | 位置 | 原因 |
|---|---|---|
| 1 | tests/test_cpp_runner.py:16 | g++ not available; skipping C++ runner tests |
| 2 | tests/test_cpp_runner.py:27 | 同上 |
| 3 | tests/test_cpp_runner.py:34 | 同上 |
| 4 | tests/test_cpp_runner.py:44 | 同上 |
| 5 | tests/test_r_runner.py:13 | Rscript not available; skipping R runner tests |
| 6 | tests/test_r_runner.py:19 | 同上 |
| 7 | tests/test_r_runner.py:25 | 同上 |

机制：两文件顶部 `pytestmark = pytest.mark.skipif(not _gpp_available()/not _r_available(), …)`（test_cpp_runner.py:13／test_r_runner.py:10）——模块级条件跳过，工具链装好后 7 条自动恢复执行，**无需改任何测试**。

## 三、口径纪律

- 对外表述统一为「**384 passed / 7 skipped（环境跳过，零 failed）**」；禁止把 7 说成 failed/挂；
- plan.md:33 与 learning-metrics-recheck-20260917.md 的表述与本次实测一致，无需订正。

## 四、零改动声明

仅运行 pytest（只读测试）＋本报告落盘；未改任何测试/源码；未 commit/push。
