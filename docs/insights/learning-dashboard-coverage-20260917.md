# learning dashboard 覆盖率复核＋未覆盖段清单（917-37，只读零改动）

**结论：复跑覆盖率命令（9c66c6d 报告同款）＝dashboard.py 59%（基线 9%→59%，持平无漂移）；未覆盖段按「异常分支／渲染分支／工具函数」三组清点完毕，每组补测可行性判定如下。源码零改动（diff 为空）。**

## 一、复跑（命令与数字同段）

```
$ python -m pytest tests/test_dashboard*.py -q --cov=ui.pages.dashboard --cov-report=term-missing
ui\pages\dashboard.py  345 stmts  126 miss  106 branch  26 partial  59%
（与 learn-dashboard-coverage-20260917.md 记载 59% 一致——差值 0）
```

## 二、未覆盖段清单（行号区段＋分组＋判定）

| 组 | 区段（miss/partial） | 分组 | 离线 fixture 可覆盖？ |
|---|---|---|---|
| 异常分支 | 39-40、78、214->217、230->250、281->283、284->286、288->286 | 异常分支（数据缺失/类型异常路径） | **可**——坏数据 fixture 即可逐段打 |
| 渲染分支 | 109-155（主管渲染体）、167-173/176-182/185-205（卡片群）、232-244/252/259-260、311-323、335-338、341、345-366、387-388、402-409、449-450、468-475、483-484、501->508、509-511、519-520 | 渲染分支（Streamlit 调用与分档展示） | **可（需 st.mock 层）**——existing 8 用例已带 st mock 样式，补渲染分支即续写 |
| 工具函数 | 501-508→509-511（partial 汇总） | 工具函数 | 可——纯函数直测 |

## 三、验收情况

- ✅ 覆盖率数字与命令同段（59%，与 59% 基线差值 0）；✅ 未覆盖段行号区段清单＋三组分组；✅ 每组判定一句；✅ 源码零改动（diff 为空）；不 push。

## 遗留问题

渲染分支的 st.mock 补测优先级最高（109-155 主管渲染体），留后续补测单。
