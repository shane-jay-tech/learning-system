# learning 窄窗（<1000px）侧栏关键入口可达性（d913c-12，2026-09-13）

任务：检查 <1000px 时侧栏关键入口（导航、当前任务）可达性，必要时加窄窗降级（不删入口）。

## 源码级可达性逐条结论（<1000px）

| 关键入口 | 实现位置 | 可达性结论 |
|---|---|---|
| 学习导航（主页/路径/诊断） | app.py:33-44（nav_button） | ✓ 常驻侧栏，无条件渲染 |
| 按语言练习（当前任务入口，7 项） | app.py:45-52 | ✓ 常驻；当前项 primary 高亮（nav_is_active 单源） |
| 复盘与巩固（面板/错题本） | app.py:54-60 | ✓ 常驻 |
| 侧栏本体（收起态再展开） | Streamlit 内建收起把手（«/»） | ✓ 框架级永远提供，窄窗收起后可一键展开 |

风险面＝侧栏默认宽（约 244px 级）在 <1000px 视口挤压主内容（内容区实得 <750px，卡片/表格横向溢出）；入口本身不消失，但「可见性/可达成本」上升。

## 改动（ui/styles.py:308-316 新增 @media (max-width: 1000px) 块）

- 侧栏自宽收敛：`width/min-width: 218px`（框架默认再收一档，主内容多让出 ~26px）；
- 侧栏内距收紧（padding .6rem .5rem 2rem）、导航按钮 min-height 42→38px、字号 .88rem、品牌/分组标签缩号；
- **零入口删除**：7 个导航按钮 + 当前任务高亮全部保留，仅几何降级；38px 仍 ≥36px 触达热区下限（plan.md 热区口径）；
- 与既有断点链（1280 wrap / 900 堆叠）正交：900px 以下主区列堆叠规则照旧，侧栏规则独立生效。

## 验收

- `python -m pytest -q`：**349 passed + 7 skipped，零失败**（≥304 ✓）；纯 CSS，零逻辑变更。
- 报告即「<1000px 逐条可达性结论 + 改动 file:line」✓。

## 遗留问题（partial 事由）

- **无浏览器实测**：<1000px 实机观感（把手点击、218px 下按钮文字不折行、主内容宽度体感）无法本地验证——按任务书失败处理条款以源码级判断收口并**标 partial**；目检归日间 Playwright/真人批次，量测点=侧栏实际渲染宽、按钮 min-height、主内容可读宽。
- Streamlit 框架把手位置（左上）不受主题控——如实测仍觉不可见，候选方案=初始 collapsed 条件化（需 session 逻辑，涉路由代码，留拍板）。
