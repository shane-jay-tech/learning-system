# 调试与日志定位

Agent 出错时，你需要能定位问题在哪一层：

- **四层定位**：依赖问题 / 路径问题 / 逻辑 bug / LLM 返回异常
- **日志阅读**：找关键报错行、追溯调用栈
- **常见模式**：ModuleNotFoundError = 依赖、FileNotFoundError = 路径
- **练习方法**：给一段错误日志，判断问题类型并建议修复

不会调试 = 依赖 Agent 调试 = 无限循环。
